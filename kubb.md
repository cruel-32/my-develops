# Kubb Configuration Guide

Kubb는 OpenAPI 스펙에서 자동으로 타입-안전한 API 클라이언트 코드를 생성하는 도구입니다.
이 문서는 프로젝트의 kubb.config.ts 설정을 상세히 설명합니다.

---

## 📋 목차

1. [현재 설정](#현재-설정)
2. [pluginReactQuery 옵션 상세](#pluginreactquery-옵션-상세)
3. [추가 사용 가능 옵션](#추가-사용-가능-옵션)
4. [옵션별 우선순위](#옵션별-우선순위)
5. [권장 설정](#권장-설정)
6. [사용 예시](#사용-예시)

---

## 현재 설정

### kubb.config.ts 전체 구조

```typescript
import { defineConfig } from '@kubb/core';
import { pluginOas } from '@kubb/plugin-oas';
import { pluginReactQuery } from '@kubb/plugin-react-query';
import { pluginTs } from '@kubb/plugin-ts';
import { pluginZod } from '@kubb/plugin-zod';

export default defineConfig({
  input: {
    path: './apps/backend/src/swagger-output.json',
  },
  output: {
    path: './packages/api/src/generated',
    clean: true,
  },
  plugins: [
    pluginOas({
      validate: false,
    }),
    pluginTs(),
    pluginZod(),
    pluginReactQuery({
      output: {
        path: './hooks',
      },
      group: {
        type: 'tag',
        name: ({ group }) => `${group}Hooks`,
      },
      client: {
        importPath: '@repo/api/client',
        dataReturnType: 'full',
      },
      mutation: {
        methods: ['post', 'put', 'delete'],
      },
      query: {
        methods: ['get'],
        importPath: '@tanstack/react-query',
      },
      infinite: {
        queryParam: 'next_page',
        initialPageParam: 0,
        cursorParam: 'nextCursor',
      },
      suspense: {},
    }),
  ],
});
```

---

## pluginReactQuery 옵션 상세

### 1. **output** (생성 경로 및 배럴 파일 설정)

```typescript
output: {
  path: './hooks',
}
```

**설명:**
- 생성된 React Query hooks 파일들이 들어갈 디렉토리
- 상대경로: `./hooks` = `packages/api/src/generated/hooks/`
- Kubb이 `useGetApi...`, `usePostApi...` 같은 hooks 파일들을 이 경로에 생성

**전체 옵션:**
```typescript
output: {
  path: './hooks',                    // 생성 경로 (필수)
  barrelType: 'named' | 'all' | 'propagate' | false,  // 배럴 파일 타입
  banner: string | (options) => string,  // 파일 상단 주석
  footer: string | (options) => string,  // 파일 하단 주석
}
```

| 속성 | 기본값 | 설명 |
|------|--------|------|
| `path` | `'hooks'` | 생성할 hooks 디렉토리 |
| `barrelType` | `'named'` | `'all'`: `export *`, `'named'`: named exports, `'propagate'`: 폴더 전파, `false`: 비활성화 |
| `banner` | 없음 | 생성된 모든 파일 상단에 추가될 텍스트 |
| `footer` | 없음 | 생성된 모든 파일 하단에 추가될 텍스트 |

**예시:**
```typescript
output: {
  path: './hooks',
  barrelType: 'named',
  banner: '/* Generated by Kubb - DO NOT EDIT */',
  footer: (options) => `/* Generated at ${new Date().toISOString()} */`
}
```

---

### 2. **group** (파일 조직화)

```typescript
group: {
  type: 'tag',
  name: ({ group }) => `${group}Hooks`,
}
```

**설명:**
- Swagger의 tag 필드를 기준으로 파일을 그룹화
- 예: `@swagger /api/auth/login @tag auth` → auth 그룹으로 분류

| 값 | 동작 |
|-----|-------|
| `type: 'tag'` | OpenAPI 태그로 그룹화 (권장) |
| `type: 'path'` | API 경로 구조로 그룹화 |

**name 함수:**
- `auth` 그룹 → `authHooks` 파일명
- `users` 그룹 → `usersHooks` 파일명
- `projects` 그룹 → `projectsHooks` 파일명

**예시:**
```typescript
group: {
  type: 'tag',
  name: ({ group }) => `${group}Hooks`  // [group]Hooks.ts
}
```

---

### 3. **client** (API 클라이언트 설정)

```typescript
client: {
  importPath: '@repo/api/client',
  dataReturnType: 'full',
}
```

**설명:**
- Kubb이 생성한 hooks가 사용할 커스텀 fetch 클라이언트 설정
- `@repo/api/client`에서 export된 `apiClient`를 자동으로 주입

| 속성 | 기본값 | 설명 |
|------|--------|------|
| `importPath` | `'@kubb/plugin-client/clients/axios'` | 클라이언트 라이브러리 경로 |
| `dataReturnType` | `'data'` | `'data'`: 데이터만, `'full'`: 전체 응답 객체 |
| `baseURL` | 없음 | API 기본 URL (선택사항) |

**예시:**
```typescript
client: {
  importPath: '@/shared/api/client',
  dataReturnType: 'full',
  baseURL: 'https://api.example.com'
}
```

**현재 설정의 의미:**
- `importPath: '@repo/api/client'` → `packages/api/src/client.ts`의 `apiClient` 사용
- `dataReturnType: 'full'` → 응답이 `{ status, data, error }` 전체 객체로 반환
- 덕분에 생성된 hooks가 Content-Type 헤더, 토큰 등이 자동으로 주입됨

---

### 4. **mutation** (POST/PUT/DELETE 훅 설정)

```typescript
mutation: {
  methods: ['post', 'put', 'delete'],
}
```

**설명:**
- REST API의 어떤 메서드가 mutation hook으로 생성될지 정의
- POST, PUT, DELETE는 side effect가 있으므로 mutation으로 처리

| 메서드 | 생성되는 훅 |
|--------|-----------|
| `POST /api/auth/login` | `usePostApiAuthLogin` |
| `PUT /api/projects/:id` | `usePutApiProjectsId` |
| `DELETE /api/projects/:id` | `useDeleteApiProjectsId` |

**예시:**
```typescript
mutation: {
  methods: ['post', 'put', 'delete', 'patch'],  // patch도 추가 가능
  importPath: '@tanstack/react-query'  // 선택사항
}
```

---

### 5. **query** (GET 훅 설정)

```typescript
query: {
  methods: ['get'],
  importPath: '@tanstack/react-query',
}
```

**설명:**
- REST API의 GET 메서드가 query hook으로 생성됨
- 데이터 조회는 side effect가 없으므로 query로 처리

| 메서드 | 생성되는 훅 |
|--------|-----------|
| `GET /api/users` | `useGetApiUsers` |
| `GET /api/users/:id` | `useGetApiUsersId` |

**importPath:**
- React Query의 `useQuery` hook을 import할 경로
- 보통 `@tanstack/react-query`로 고정

---

### 6. **infinite** (무한 스크롤/페이지네이션 설정)

```typescript
infinite: {
  queryParam: 'next_page',
  initialPageParam: 0,
  cursorParam: 'nextCursor',
}
```

**설명:**
- 무한 스크롤 기능을 지원하는 useInfiniteQuery 훅 생성
- 페이지네이션 API를 위한 설정

| 속성 | 설명 | 예시 |
|------|------|------|
| `queryParam` | 다음 페이지를 요청할 쿼리 파라미터 이름 | `?next_page=2` |
| `initialPageParam` | 첫 요청시 초기 page 값 | `0` (0부터 시작) |
| `cursorParam` | 응답에서 다음 페이지 정보를 가져올 필드명 | `{ data: [...], nextCursor: 2 }` |

**생성되는 훅:**
```typescript
const { data, fetchNextPage } = useInfiniteGetApiUsersList({
  initialPageParam: 0,
  getNextPageParam: (lastPage) => lastPage.nextCursor
})
```

**비활성화:**
```typescript
infinite: false  // 무한 쿼리 생성 안함
```

---

### 7. **suspense** (React Suspense 통합)

```typescript
suspense: {}
```

**설명:**
- React의 Suspense와 통합되는 hook 생성
- 로딩 중일 때 자동으로 throw하여 가장 가까운 Suspense boundary에서 처리

**생성되는 훅:**
```typescript
// 일반 훅
const { data } = useGetApiUsers()

// Suspense 훅 (데이터 로딩 중이면 throw)
const { data } = useSuspenseGetApiUsers()
```

**비활성화:**
```typescript
suspense: false  // Suspense 훅 생성 안함
```

---

## 추가 사용 가능 옵션

### **contentType** (컨텐츠 타입)

```typescript
contentType: 'application/json'
```

요청/응답의 기본 content type 정의합니다.

---

### **include** (포함 필터)

```typescript
include: [
  { type: 'tag', pattern: 'users' },           // users 태그만
  { type: 'method', pattern: /get|post/i },    // GET, POST만
  { type: 'path', pattern: '/api/v1/*' }       // /api/v1 경로만
]
```

**type 옵션:**
- `'tag'`: OpenAPI 태그로 필터
- `'operationId'`: operationId로 필터
- `'path'`: API 경로로 필터
- `'method'`: HTTP 메서드로 필터
- `'contentType'`: Content-Type으로 필터

특정 엔드포인트만 hook 생성합니다.

---

### **exclude** (제외 필터)

```typescript
exclude: [
  { type: 'tag', pattern: 'internal' },        // internal 태그 제외
  { type: 'method', pattern: 'delete' },       // DELETE 메서드 제외
  { type: 'path', pattern: /\/admin\// }       // /admin 경로 제외
]
```

특정 엔드포인트 생성을 스킵합니다.

---

### **override** (조건부 재정의)

```typescript
override: [
  {
    type: 'tag',
    pattern: 'auth',
    options: {
      mutation: { methods: ['post'] },         // auth 태그는 POST만
      client: { dataReturnType: 'full' }       // full 응답
    }
  },
  {
    type: 'path',
    pattern: '/api/admin/*',
    options: {
      exclude: true  // admin API는 제외
    }
  }
]
```

패턴별로 다른 설정을 적용합니다.

---

### **paramsCasing** (파라미터 이름 스타일)

```typescript
paramsCasing: 'camelcase'  // user_id → userId로 변환
```

파라미터 이름 스타일을 변경합니다.

---

### **paramsType** (파라미터 전달 방식)

```typescript
paramsType: 'object' | 'inline'  // 기본값: 'inline'
```

| 값 | 예시 |
|-----|------|
| `'inline'` | `getUser(id, name, email)` |
| `'object'` | `getUser({ id, name, email })` |

---

### **pathParamsType** (URL 경로 파라미터 전달 방식)

```typescript
pathParamsType: 'object' | 'inline'  // 기본값: 'inline'
```

URL 경로 파라미터 전달 방식을 정의합니다.

---

### **parser** (데이터 파서)

```typescript
parser: 'client' | 'zod'  // 기본값: 'client'
```

| 값 | 설명 |
|-----|------|
| `'client'` | 검증 없음, 원본 데이터 반환 |
| `'zod'` | Zod 스키마로 자동 검증 (requires @kubb/plugin-zod) |

**Zod 사용 예시:**
```typescript
parser: 'zod'  // @kubb/plugin-zod 필수
```

---

### **queryKey** (커스텀 쿼리 키)

```typescript
queryKey: ({ operation, schemas, casing }) => {
  return [operation.operationId, 'v1']
}
```

React Query의 queryKey 생성 방식을 커스터마이징합니다.

**파라미터:**
- `operation`: OpenAPI 작업 객체
- `schemas`: 스키마 정보
- `casing`: 파라미터 케이싱 스타일

---

### **mutationKey** (커스텀 뮤테이션 키)

```typescript
mutationKey: ({ operation, schemas, casing }) => {
  return [operation.operationId, 'mutation']
}
```

React Query의 mutationKey 생성 방식을 커스터마이징합니다.

---

### **transformers** (생성 이름 변환)

```typescript
transformers: {
  name: (name, type) => {
    if (type === 'file') return `${name}.generated`
    if (type === 'function') return `use${name}`
    if (type === 'type') return `${name}Type`
    if (type === 'const') return `${name}Const`
    return name
  }
}
```

**type 가능한 값:**
- `'file'`: 생성된 파일명
- `'function'`: 함수명
- `'type'`: 타입명
- `'const'`: 상수명

생성 파일, 함수, 타입의 이름 규칙을 변경합니다.

---

### **generators** (커스텀 제너레이터)

```typescript
generators: [customGenerator]  // 고급 기능
```

자동 생성 로직을 완전히 커스터마이징합니다.

---

## 옵션별 우선순위

| 옵션 | 사용 빈도 | 필수 여부 | 용도 |
|------|----------|---------|------|
| `output` | 매우 높음 | 예 | 생성 경로 및 배럴 파일 설정 |
| `client` | 매우 높음 | 예 | API 클라이언트 설정 |
| `query`, `mutation` | 높음 | 예 | HTTP 메서드별 훅 생성 |
| `group` | 높음 | 예 | 파일 조직화 |
| `include`, `exclude` | 중간 | 아니오 | 특정 엔드포인트 필터링 |
| `infinite`, `suspense` | 중간 | 아니오 | 고급 쿼리 기능 |
| `override` | 낮음 | 아니오 | 예외 처리 |
| `transformers` | 낮음 | 아니오 | 이름 커스터마이징 |
| `parser` | 낮음 | 아니오 | Zod 통합 (optional) |
| `generators` | 매우 낮음 | 아니오 | 고급 커스터마이징 |

---

## 권장 설정

### 기본 설정 (현재)

```typescript
pluginReactQuery({
  output: {
    path: './hooks',
  },
  group: {
    type: 'tag',
    name: ({ group }) => `${group}Hooks`,
  },
  client: {
    importPath: '@repo/api/client',
    dataReturnType: 'full',
  },
  mutation: {
    methods: ['post', 'put', 'delete'],
  },
  query: {
    methods: ['get'],
    importPath: '@tanstack/react-query',
  },
  infinite: {
    queryParam: 'next_page',
    initialPageParam: 0,
    cursorParam: 'nextCursor',
  },
  suspense: {},
})
```

---

### 향상된 설정 (권장)

```typescript
pluginReactQuery({
  output: {
    path: './hooks',
    barrelType: 'named',
    banner: '/* Generated by Kubb - Do not edit manually */',
  },
  group: {
    type: 'tag',
    name: ({ group }) => `${group}Hooks`,
  },
  client: {
    importPath: '@repo/api/client',
    dataReturnType: 'full',
  },
  mutation: {
    methods: ['post', 'put', 'delete', 'patch'],
  },
  query: {
    methods: ['get'],
    importPath: '@tanstack/react-query',
  },
  infinite: {
    queryParam: 'next_page',
    initialPageParam: 0,
    cursorParam: 'nextCursor',
  },
  suspense: {},

  // 추가 옵션
  exclude: [
    { type: 'tag', pattern: 'internal' }
  ],
  transformers: {
    name: (name, type) => {
      if (type === 'function') return `use${name}`
      return name
    }
  },
})
```

---

## 사용 예시

### 생성된 훅 사용 방법

```typescript
// 1. 로그인 (mutation)
import { usePostApiAuthLogin } from '@repo/api'

const { mutate, isPending } = usePostApiAuthLogin({
  mutation: {
    onSuccess: (data) => {
      // data: PostApiAuthLoginMutationResponse
      console.log(data.accessToken)
    },
    onError: (error) => {
      // error: PostApiAuthLogin400 | PostApiAuthLogin401 | PostApiAuthLogin404
      console.log(error.error)
    }
  }
})

mutate({ data: { email: 'user@example.com', password: 'password' } })
```

```typescript
// 2. 사용자 목록 조회 (query)
import { useGetApiUsersList } from '@repo/api'

const { data, isLoading, error } = useGetApiUsersList()
// data: User[]
```

```typescript
// 3. 무한 목록 (infinite query)
import { useInfiniteGetApiUsersList } from '@repo/api'

const { data, fetchNextPage, hasNextPage } = useInfiniteGetApiUsersList({
  initialPageParam: 0,
})

// data.pages 접근
data?.pages.forEach(page => {
  console.log(page.users)
})
```

---

## 프로젝트 구조와의 연관

```
kubb.config.ts
    ↓
    ├─ input: Swagger JSON 읽기
    │   └── apps/backend/src/swagger-output.json
    │
    ├─ pluginOas: OpenAPI 스펙 검증
    │
    ├─ pluginTs: TypeScript 타입 생성
    │   └── packages/api/src/generated/types/
    │
    ├─ pluginZod: Zod 스키마 생성
    │   └── packages/api/src/generated/zod/
    │
    └─ pluginReactQuery: React Query 훅 생성
        ├── output.path: packages/api/src/generated/hooks/
        ├── client.importPath: packages/api/src/client.ts
        └── group.type: Swagger tag별로 파일 분류
            ├── authHooks.ts (POST /api/auth/login, etc)
            ├── usersHooks.ts (GET /api/users, etc)
            └── projectsHooks.ts (GET/POST /api/projects, etc)

packages/api/src/index.ts
    └── 생성된 hooks & types export
        └── @repo/api 패키지로 사용 가능

@apps/web/ (프론트엔드)
    └── import { usePostApiAuthLogin } from '@repo/api'
```

---

## 주의사항

1. **kubb 재생성 시**
   - `output.clean: true` 설정으로 인해 `packages/api/src/generated/` 폴더 전체 삭제됨
   - custom hooks는 `packages/api/src/hooks/` 폴더에 별도 관리 (Kubb이 삭제하지 않음)

2. **Swagger 스펙 변경 후**
   - `pnpm kubb generate` 실행하여 새로운 hooks 생성
   - Type 변경사항 확인

3. **Custom Client 설정**
   - `client.importPath`가 정확해야 모든 hooks에 자동으로 주입됨
   - Content-Type, Authorization 헤더 등이 자동으로 처리됨

4. **importPath 주의**
   - `@repo/api/client` = `packages/api/src/client.ts`
   - 절대경로 alias 사용 권장

---

## 참고 자료

- **Kubb 공식 문서:** https://kubb.dev
- **TanStack Query 문서:** https://tanstack.com/query
- **OpenAPI 스펙:** https://swagger.io/specification/
- **프로젝트 kubb.config.ts:** `/kubb.config.ts`

---

**마지막 업데이트:** 2025-10-25
